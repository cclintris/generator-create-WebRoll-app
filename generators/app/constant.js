const APP_TYPE = {
  WEBPACK: 'webpack',
  ROLLUP: 'rollup',
}

const BOXEN_OPTS = {
  padding: 1,
  margin: 1,
  align: 'center',
  borderColor: 'yellow',
  borderStyle: 'round',
}

const DEFAULT_DIR = 'webpack-app'

module.exports = {
  APP_TYPE,
  BOXEN_OPTS,
  DEFAULT_DIR,
}
